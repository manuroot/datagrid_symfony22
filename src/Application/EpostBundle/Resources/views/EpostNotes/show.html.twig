{% extends "ApplicationEpostBundle::layout.html.twig" %}

{% block javascripts %}
{{ parent() }}
 {{ tinymce_init() }}
{% endblock %}

  {% block stylesheets %}
{{ parent() }}
      <link rel="stylesheet" href="{{ asset('bundles/applicationepost/css/sliding.css') }}" type="text/css" media="all"/>
     
 {% endblock %}

{% block content_header '' %}
{% block title %}
Notes
{% endblock %}

{% block content %}
     <br>  
<table class="table table-striped table-bordered table-hover">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
             
            </tr>
            <tr>
                <th>Note</th>
                <td>{{ entity.note }}</td>
            </tr>
            <tr>
                <th>Post</th>
                <td>{{ entity.epost.name }}</td>
            </tr>
            <tr>
                <th>Proprietaire</th>
                <td>{{ entity.epost.proprietaire.username }}</td>
            </tr>
            
        </tbody>
    </table>

     <br>
     
        <a href="{{ path('epostnotes')  }}" class="btn btn-small btn-success">Retour</a>
{#<a href="{{ path('eproduit_edit', { 'id': entity.id }) }}" class="btn btn-small btn-warning">Editer</a>#}
<br><br>

{#

      
    <li>
        <form action="{{ path('epostnotes_delete', { 'id': entity.id }) }}" method="post">
            <input type="hidden" name="_method" value="DELETE" />
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
#}
{% endblock %}
