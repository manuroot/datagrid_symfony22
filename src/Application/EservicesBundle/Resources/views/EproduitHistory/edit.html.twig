{% extends "ApplicationEservicesBundle::layout.html.twig" %}

     
  {% block javascripts %}
{{ parent() }}
   <!--<script type="text/javascript" src="{{ asset('bundles/applicationeservices/js/selectapplis.js') }}"></script>
    -->
<script type="text/javascript">
$(document).ready(function() {
//$(function() {
  $( "#eproduit_history_type_dateIn" ).datepicker({
maxDate: "+3M +10D",
changeMonth: true,
changeYeat: true,
numberOfMonths: 1,
dateFormat: "yy-mm-dd 12:00",
onClose: function( selectedDate ) {
$( "#form_bis" ).datepicker( "option", "minDate", selectedDate );
}
});
   $( "#eproduit_history_type_dateOut" ).datepicker({
maxDate: "+3M +10D",
changeMonth: true,
changeYeat: true,
numberOfMonths: 1,
dateFormat: "yy-mm-dd 12:00",
onClose: function( selectedDate ) {
$( "#form_bis" ).datepicker( "option", "minDate", selectedDate );
}
});
});
</script>
 {{ tinymce_init() }}
{% endblock %}

{% block content_header '' %}
{% block title %}
PRET-PRODUIT
  {% if entity.produit.proprietaire %} ({{ entity.produit.proprietaire }}){% endif %}
              
{% endblock %}



{% block content %}
  
  
    
   <form action="{{ path('ep_history_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
     <table class="table table-bordered">
    <tbody>      
         
        <tr><td>
                 <div>
                  {{ form_row(edit_form.dateIn) }}
                           {{ form_row(edit_form.dateOut) }}
        
                 {#    {{ form_row(edit_form.services) }}#}
              </div>
                     </td>
 <td>
 <input type="hidden" name="_method" value="PUT" />
{{ form_rest(edit_form) }} </td>
       
                                 </tr>
                                     
<tr><td colspan="2">
                                   
  <button type="submit" class="btn btn-small btn-warning">Modifier</button>
  
    <a href="{{ path('ep_history')  }}" class="btn btn-small btn-success">Retour</a>
   </form>       
                                    
                                       
                                </td></tr>
                        </tbody>
                    </table>
                
 
  
        
   
   {#     <form action="{{ path('ep_history_delete', { 'id': entity.id }) }}" method="post">
            <input type="hidden" name="_method" value="DELETE" />
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
#}
{% endblock %}
